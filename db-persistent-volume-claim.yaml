apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data-volume-claim
spec:
  volumeName: db-data-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
